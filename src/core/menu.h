// Launcher menu: lists every registered app, tap to launch, drag to scroll
// when the list outgrows the screen. Shows battery and clock in a status bar.
#pragma once

#include "app.h"

namespace core::menu {

App& instance();

} // namespace core::menu
